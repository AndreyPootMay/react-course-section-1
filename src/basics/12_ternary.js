
const active = true;

// let msg = '';

// if ( !active ) {
//     msg = 'Active';
// } else {
//     msg = 'Inactive';
// }
// const msg = ( active ) ? 'active' : 'Inactive'; 
// const msg = ( active ) ? 'active' : null; 
const msg = active && 'Activo';


console.log(msg);